{
  "name": "@newordergame/common",
  "version": "1.0.56",
  "description": "",
  "main": "index.js",
  "module": "index.ts",
  "types": "index.d.ts",
  "scripts": {
    "start": "yarn watch",
    "build": "npx tsc",
    "watch": "npx tsc --watch --noEmit | sed -u 's/\\x1Bc//g'",
    "test": "yarn build && npx jest",
    "ship": "yarn publish-package",
    "publish-package": "yarn build && yarn publish --patch && yarn update-package",
    "upgrade-package": "cd ../../ && yarn upgrade-common && cd -",
    "prettier": "npx prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.2.1",
    "jest": "^29.5.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "mathjs": "^11.3.2",
    "pino": "^8.5.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.1",
    "yup": "^0.32.11"
  }
}
