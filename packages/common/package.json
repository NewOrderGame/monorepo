{
  "name": "@newordergame/common",
  "version": "1.0.54",
  "description": "",
  "main": "index.js",
  "module": "index.ts",
  "types": "index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "test": "yarn build && npx jest",
    "deploy-package": "yarn build && yarn publish",
    "update-common": "cd ../core && yarn update-common; cd ../ui && yarn update-common; cd ../common ",
    "prettier": "npx prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.2.1",
    "jest": "^29.5.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "mathjs": "^11.3.2",
    "pino": "^8.5.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.1",
    "yup": "^0.32.11"
  }
}
