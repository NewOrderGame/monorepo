{
  "name": "@newordergame/encounter",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "yarn dev",
    "dev": "shx cp environment/.env.development .env && nodemon",
    "debug": "shx cp environment/.env.development .env && nodemon --config ./nodemon.debug.json",
    "deploy": "yarn citadel:deploy",
    "docker:build": "docker build -t encounter-service .",
    "docker:login": "docker login",
    "docker:tag": "docker tag encounter-service devlysh/newordergame-encounter",
    "docker:push": "docker push devlysh/newordergame-encounter",
    "docker:pull": "docker pull devlysh/newordergame-encounter",
    "citadel:deploy": "yarn docker:build && yarn docker:tag && yarn docker:login && yarn docker:push && yarn citadel:restart",
    "citadel:restart": "yarn citadel:stop && yarn citadel:rm && yarn citadel:pull && yarn citadel:start",
    "citadel:start": "ssh $CITADEL_USER@$CITADEL_IP_ADDRESS docker run -d --name newordergame-encounter devlysh/newordergame-encounter",
    "citadel:stop": "ssh $CITADEL_USER@$CITADEL_IP_ADDRESS docker stop newordergame-encounter",
    "citadel:rm": "ssh $CITADEL_USER@$CITADEL_IP_ADDRESS docker rm newordergame-encounter",
    "citadel:login": "ssh $CITADEL_USER@$CITADEL_IP_ADDRESS docker login",
    "citadel:pull": "ssh $CITADEL_USER@$CITADEL_IP_ADDRESS docker pull devlysh/newordergame-encounter",
    "webpack:build": "cross-env NODE_ENV=production shx cp environment/.env.production .env && webpack --mode=production",
    "webpack:watch": "shx cp environment/.env.development .env && webpack --watch",
    "lint": "eslint ./src --config ./.eslintrc.js --no-cache",
    "format": "prettier --write src/**/*.ts",
    "test": "shx cp environment/.env.development .env && jest --detectOpenHandles --collect-coverage",
    "type-check": "tsc --noEmit",
    "verify": "yarn format && yarn type-check && yarn lint && yarn test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {}
}
